//Multiplying the length of arrays

var colors = ["pink", "red", "black", "purple"];
var rooms = ["room1", "room2", "room3", "room4"];

var combos = colors.length * rooms.length;

console.log(combos);

